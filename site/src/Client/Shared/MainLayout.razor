@inherits LayoutComponentBase
@inject IStringLocalizer<Resource> Localizer

<MudThemeProvider Theme="_theme"/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
    <MudAppBar>
        <MudIconButton 
            Icon="@Icons.Material.Filled.Menu" 
            Color="Color.Inherit" 
            Edge="Edge.Start" 
            OnClick="@(DrawerToggle)" />
        @Localizer[nameof(Resource.CompanyName)]
        <MudSpacer />
        <MudIconButton Icon="@Icons.Custom.Brands.GitHub" Color="Color.Inherit" Href="https://github.com/badbredren/ContactSite" Target="_blank"/>
    </MudAppBar>

    <MudDrawer @bind-Open="@_drawerOpen">
        <NavMenu />
    </MudDrawer>
    
    <MudMainContent MaxWidth="MaxWidth.Medium">
        @Body
    </MudMainContent>
    
    <MudAppBar Bottom="true" Fixed="true">
        <CultureSelector />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.FavoriteBorder" Color="Color.Inherit" Edge="Edge.End" />
        <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" />
    </MudAppBar>
</MudLayout>

@code {
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

    readonly MudTheme _theme = new()
    {
        Typography = new()
        {
            Default = new()
            {
                FontFamily = new [] {"Arimo", "Helvetica", "Arial", "sans-serif"}
            }
        }
    };

}